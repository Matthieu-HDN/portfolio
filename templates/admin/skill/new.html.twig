{% extends 'base.html.twig' %}

{% block title %}Nouvelle compétence{% endblock %}

{% block body %}
<div class="max-w-5xl mx-auto px-6 py-16">

    <div class="flex justify-between items-center mb-10">
        <div>
            <p class="text-blue-400 text-sm uppercase tracking-widest mb-1">Administration</p>
            <h1 class="text-4xl font-bold text-white">Nouvelle compétence</h1>
        </div>
        <a href="{{ path('app_skill_index') }}" 
           class="border border-gray-700 hover:border-gray-500 text-gray-400 hover:text-white px-4 py-2 rounded-lg text-sm transition">
            ← Retour
        </a>
    </div>

    <div class="bg-gray-900 border border-gray-800 rounded-xl p-8">
        {{ include('admin/skill/_form.html.twig') }}
    </div>

</div>
{% endblock %}